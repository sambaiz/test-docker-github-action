description: 'Echo your word'
inputs:
  i-say:
    description: 'What do you say?'
    required: true
    default: '(silence)'
outputs:
  return-word:
    description: 'some number'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.i-say }}
